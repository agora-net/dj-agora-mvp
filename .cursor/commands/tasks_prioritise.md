# Find and Prioritise Issues

Use the `gh` CLI tool to list open issues for this repository assigned to me (`gh issue list --assignee "@me"`).

For each issue, check if it's in @docs/tasks.md already and if not write into @/docs/tasks.md with an outline of what is required.
