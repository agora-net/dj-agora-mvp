import { createIcons, Palette, Sun, Moon, SunMoon, CircleUser, Fingerprint, IdCard, HatGlasses } from 'lucide';

createIcons({
  icons: {
    Palette,
    Sun,
    Moon,
    SunMoon,
    CircleUser,
    Fingerprint,
    IdCard,
    HatGlasses
  }
});